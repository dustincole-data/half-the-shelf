# Keeps a CLI deploy from uploading the working tree. Do NOT list out/ here:
# out/site/index.html is the whole deployment (see vercel.json), and an
# `out/*` + `!out/site/` pair removed it, which built an empty output and
# served 404 at the root.
.chrome-profile/
__pycache__/
data/
